import { Interceptor } from '@grpc/grpc-js';
import { RetryStrategy } from '../../config/retry/retry-strategy';
import { MomentoLoggerFactory } from '../../';
export declare function createRetryInterceptorIfEnabled(loggerFactory: MomentoLoggerFactory, retryStrategy: RetryStrategy): Array<Interceptor>;
export declare class RetryInterceptor {
    private readonly logger;
    private readonly retryStrategy;
    constructor(loggerFactory: MomentoLoggerFactory, retryStrategy: RetryStrategy);
    createRetryInterceptor(): Interceptor;
}
