import { CredentialProvider, VectorIndexConfiguration } from '..';
import { CreateVectorIndex, DeleteVectorIndex, ListVectorIndexes } from '@gomomento/sdk-core';
import { IVectorIndexControlClient, VectorSimilarityMetric } from '@gomomento/sdk-core/dist/src/internal/clients';
export interface ControlClientProps {
    configuration: VectorIndexConfiguration;
    credentialProvider: CredentialProvider;
}
export declare class VectorIndexControlClient implements IVectorIndexControlClient {
    private readonly clientWrapper;
    private readonly interceptors;
    private static readonly REQUEST_TIMEOUT_MS;
    private readonly logger;
    private readonly cacheServiceErrorMapper;
    /**
     * @param {ControlClientProps} props
     */
    constructor(props: ControlClientProps);
    createIndex(indexName: string, numDimensions: number, similarityMetric?: VectorSimilarityMetric): Promise<CreateVectorIndex.Response>;
    listIndexes(): Promise<ListVectorIndexes.Response>;
    deleteIndex(name: string): Promise<DeleteVectorIndex.Response>;
}
