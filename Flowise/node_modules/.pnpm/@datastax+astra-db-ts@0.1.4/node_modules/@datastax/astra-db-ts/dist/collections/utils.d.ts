import { HTTPClient } from "../client/httpClient";
interface ParsedUri {
    baseUrl: string;
    baseApiPath: string;
    keyspaceName: string;
    applicationToken: string;
    logLevel: string;
    authHeaderName: string;
}
export declare const parseUri: (uri: string) => ParsedUri;
export declare function getKeyspaceName(pathFromUrl?: string | null): string;
/**
 * Create an Astra connection URI while connecting to Astra JSON API
 * @param apiEndPoint the API EndPoint of the Astra database
 * @param keyspace the keyspace to connect to
 * @param applicationToken an Astra application token
 * @param baseApiPath baseAPI path defaults to /api/json/v1
 * @param logLevel an winston log level (error: 0, warn: 1, info: 2, http: 3, verbose: 4, debug: 5, silly: 6)
 * @param authHeaderName
 * @returns URL as string
 */
export declare function createAstraUri(apiEndPoint: string, keyspace: string, applicationToken?: string, baseApiPath?: string, logLevel?: string, authHeaderName?: string): string;
/**
 * Create a JSON API connection URI while connecting to Open source JSON API
 * @param baseUrl the base URL of the JSON API
 * @param baseAuthUrl the base URL of the JSON API auth (this is generally the Stargate Coordinator auth URL)
 * @param keyspace the keyspace to connect to
 * @param username the username to connect with
 * @param password the password to connect with
 * @param logLevel an winston log level (error: 0, warn: 1, info: 2, http: 3, verbose: 4, debug: 5, silly: 6)
 * @returns URL as string
 */
export declare function createStargateUri(baseUrl: string, baseAuthUrl: string, keyspace: string, username: string, password: string, logLevel?: string): Promise<string>;
/**
 * Get an access token from Stargate (this is useful while connecting to open source JSON API)
 * @param authUrl the base URL of the JSON API auth (this is generally the Stargate Coordinator auth URL)
 * @param username Username
 * @param password Password
 * @returns access token as string
 */
export declare function getStargateAccessToken(authUrl: string, username: string, password: string): Promise<any>;
export declare class StargateAuthError extends Error {
    message: string;
    constructor(message: string);
}
export declare const executeOperation: (operation: () => Promise<unknown>) => Promise<any>;
export declare function createNamespace(httpClient: HTTPClient, name: string): Promise<import("../client/httpClient").APIResponse>;
export declare function dropNamespace(httpClient: HTTPClient, name: string): Promise<import("../client/httpClient").APIResponse>;
export declare function setDefaultIdForUpsert(command: Record<string, any>, replace?: boolean): void;
export {};
