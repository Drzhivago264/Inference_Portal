import { Message } from "./message_models";
import { IZepClient } from "./interfaces";
export default class MessageManager {
    client: IZepClient;
    constructor(client: IZepClient);
    getSessionMessages(sessionId: string, limit?: number, cursor?: number): Promise<Message[]>;
    getSessionMessage(sessionId: string, messageId: string): Promise<Message>;
    updateSessionMessageMetadata(sessionId: string, messageId: string, metadata: Record<string, any>): Promise<Message>;
}
