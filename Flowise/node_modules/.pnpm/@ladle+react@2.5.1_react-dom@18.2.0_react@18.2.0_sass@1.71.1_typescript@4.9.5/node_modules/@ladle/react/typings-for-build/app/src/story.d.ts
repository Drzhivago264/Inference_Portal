import * as React from "react";
import type { GlobalState, GlobalAction } from "../../shared/types";
declare const Story: ({ globalState, dispatch, }: {
    globalState: GlobalState;
    dispatch: React.Dispatch<GlobalAction>;
}) => JSX.Element | null;
export default Story;
