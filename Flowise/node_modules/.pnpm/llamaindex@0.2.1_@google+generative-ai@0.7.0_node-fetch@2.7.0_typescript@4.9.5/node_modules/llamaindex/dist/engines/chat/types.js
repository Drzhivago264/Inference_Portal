export var ChatResponseMode;
(function(ChatResponseMode) {
    ChatResponseMode["WAIT"] = "wait";
    ChatResponseMode["STREAM"] = "stream";
})(ChatResponseMode || (ChatResponseMode = {}));
export class AgentChatResponse {
    response;
    sources;
    sourceNodes;
    constructor(response, sources, sourceNodes){
        this.response = response;
        this.sources = sources || [];
        this.sourceNodes = sourceNodes || [];
    }
    _getFormattedSources() {
        throw new Error("Not implemented yet");
    }
    toString() {
        return this.response ?? "";
    }
}
export class StreamingAgentChatResponse {
    response;
    sources;
    sourceNodes;
    constructor(response, sources, sourceNodes){
        this.response = response;
        this.sources = sources ?? [];
        this.sourceNodes = sourceNodes ?? [];
    }
}
