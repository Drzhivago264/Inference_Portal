import type { GenericFileSystem } from "@llamaindex/env";
import type { BaseDocumentStore } from "./docStore/types.js";
import type { BaseIndexStore } from "./indexStore/types.js";
import type { VectorStore } from "./vectorStore/types.js";
export interface StorageContext {
    docStore: BaseDocumentStore;
    indexStore: BaseIndexStore;
    vectorStore: VectorStore;
    imageVectorStore?: VectorStore;
}
export type BuilderParams = {
    docStore: BaseDocumentStore;
    indexStore: BaseIndexStore;
    vectorStore: VectorStore;
    imageVectorStore: VectorStore;
    storeImages: boolean;
    persistDir: string;
    fs: GenericFileSystem;
};
export declare function storageContextFromDefaults({ docStore, indexStore, vectorStore, imageVectorStore, storeImages, persistDir, fs, }: Partial<BuilderParams>): Promise<StorageContext>;
