import type { BulkWriteOptions } from "mongodb";
import { MongoClient } from "mongodb";
import type { BaseNode } from "../../Node.js";
import type { VectorStore, VectorStoreQuery, VectorStoreQueryResult } from "./types.js";
export declare class MongoDBAtlasVectorSearch implements VectorStore {
    storesText: boolean;
    flatMetadata: boolean;
    mongodbClient: MongoClient;
    indexName: string;
    embeddingKey: string;
    idKey: string;
    textKey: string;
    metadataKey: string;
    insertOptions?: BulkWriteOptions;
    private collection;
    constructor(init: Partial<MongoDBAtlasVectorSearch> & {
        dbName: string;
        collectionName: string;
    });
    add(nodes: BaseNode[]): Promise<string[]>;
    delete(refDocId: string, deleteOptions?: any): Promise<void>;
    get client(): any;
    query(query: VectorStoreQuery, options?: any): Promise<VectorStoreQueryResult>;
}
