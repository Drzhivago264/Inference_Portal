import type { ChatMemoryBuffer } from "../memory/ChatMemoryBuffer.js";
import type { BaseTool } from "../types.js";
import type { TaskStep } from "./types.js";
/**
 * Adds the user's input to the memory.
 *
 * @param step - The step to add to the memory.
 * @param memory - The memory to add the step to.
 * @param verbose - Whether to print debug messages.
 */
export declare function addUserStepToMemory(step: TaskStep, memory: ChatMemoryBuffer, verbose?: boolean): void;
/**
 * Get function by name.
 * @param tools: tools
 * @param name: name
 * @returns: tool
 */
export declare function getFunctionByName(tools: BaseTool[], name: string): BaseTool;
