import type { ChatMessage } from "../../llm/index.js";
import type { BaseTool } from "../../types.js";
import type { BaseReasoningStep } from "./types.js";
export interface BaseAgentChatFormatter {
    format(tools: BaseTool[], chatHistory: ChatMessage[], currentReasoning?: BaseReasoningStep[]): ChatMessage[];
}
export declare class ReActChatFormatter implements BaseAgentChatFormatter {
    systemHeader: string;
    context: string;
    constructor(init?: Partial<ReActChatFormatter>);
    format(tools: BaseTool[], chatHistory: ChatMessage[], currentReasoning?: BaseReasoningStep[]): ChatMessage[];
}
