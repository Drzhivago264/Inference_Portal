import type { Event, EventTag, EventType } from "./callbacks/CallbackManager.js";
export declare enum Tokenizers {
    CL100K_BASE = "cl100k_base"
}
/**
 * Helper class singleton
 */
declare class GlobalsHelper {
    defaultTokenizer: {
        encode: (text: string) => Uint32Array;
        decode: (tokens: Uint32Array) => string;
    } | null;
    private initDefaultTokenizer;
    tokenizer(encoding?: Tokenizers): (text: string) => Uint32Array;
    tokenizerDecoder(encoding?: Tokenizers): (tokens: Uint32Array) => string;
    createEvent({ parentEvent, type, tags, }: {
        parentEvent?: Event;
        type: EventType;
        tags?: EventTag[];
    }): Event;
}
export declare const globalsHelper: GlobalsHelper;
export {};
