import type { CallbackManager } from "../../callbacks/CallbackManager.js";
import type { ChatMessage } from "../../llm/index.js";
import { OpenAI } from "../../llm/index.js";
import type { ObjectRetriever } from "../../objects/base.js";
import type { BaseTool } from "../../types.js";
import { AgentRunner } from "../runner/base.js";
type OpenAIAgentParams = {
    tools?: BaseTool[];
    llm?: OpenAI;
    memory?: any;
    prefixMessages?: ChatMessage[];
    verbose?: boolean;
    maxFunctionCalls?: number;
    defaultToolChoice?: string;
    callbackManager?: CallbackManager;
    toolRetriever?: ObjectRetriever;
    systemPrompt?: string;
};
/**
 * An agent that uses OpenAI's API to generate text.
 *
 * @category OpenAI
 */
export declare class OpenAIAgent extends AgentRunner {
    constructor({ tools, llm, memory, prefixMessages, verbose, maxFunctionCalls, defaultToolChoice, callbackManager, toolRetriever, systemPrompt, }: OpenAIAgentParams);
}
export {};
