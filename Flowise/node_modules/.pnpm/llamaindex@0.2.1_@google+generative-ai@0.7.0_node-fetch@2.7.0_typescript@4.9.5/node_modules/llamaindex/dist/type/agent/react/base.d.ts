import type { CallbackManager } from "../../callbacks/CallbackManager.js";
import type { ChatMessage, LLM } from "../../llm/index.js";
import type { ObjectRetriever } from "../../objects/base.js";
import type { BaseTool } from "../../types.js";
import { AgentRunner } from "../runner/base.js";
type ReActAgentParams = {
    tools: BaseTool[];
    llm?: LLM;
    memory?: any;
    prefixMessages?: ChatMessage[];
    verbose?: boolean;
    maxInteractions?: number;
    defaultToolChoice?: string;
    callbackManager?: CallbackManager;
    toolRetriever?: ObjectRetriever;
};
/**
 * An agent that uses OpenAI's API to generate text.
 *
 * @category OpenAI
 */
export declare class ReActAgent extends AgentRunner {
    constructor({ tools, llm, memory, prefixMessages, verbose, maxInteractions, defaultToolChoice, callbackManager, toolRetriever, }: Partial<ReActAgentParams>);
}
export {};
