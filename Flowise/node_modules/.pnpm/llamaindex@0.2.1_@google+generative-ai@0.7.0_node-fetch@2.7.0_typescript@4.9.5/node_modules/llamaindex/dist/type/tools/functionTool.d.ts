import type { BaseTool, ToolMetadata } from "../types.js";
type Metadata = {
    name: string;
    description: string;
    parameters: ToolMetadata["parameters"];
};
export declare class FunctionTool<T = any> implements BaseTool {
    private _fn;
    private _metadata;
    constructor(fn: (...args: any[]) => any, metadata: Metadata);
    static fromDefaults<T = any>(fn: (...args: any[]) => any, metadata?: Metadata): FunctionTool<T>;
    get metadata(): ToolMetadata;
    call(...args: any[]): Promise<any>;
}
export {};
