"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: all[name]
    });
}
_export(exports, {
    addUserStepToMemory: function() {
        return addUserStepToMemory;
    },
    getFunctionByName: function() {
        return getFunctionByName;
    }
});
function addUserStepToMemory(step, memory, verbose = false) {
    if (!step.input) {
        return;
    }
    const userMessage = {
        content: step.input,
        role: "user"
    };
    memory.put(userMessage);
    if (verbose) {
        console.log(`Added user message to memory!: ${userMessage.content}`);
    }
}
function getFunctionByName(tools, name) {
    const nameToTool = {};
    tools.forEach((tool)=>{
        nameToTool[tool.metadata.name] = tool;
    });
    if (!(name in nameToTool)) {
        throw new Error(`Tool with name ${name} not found`);
    }
    return nameToTool[name];
}
