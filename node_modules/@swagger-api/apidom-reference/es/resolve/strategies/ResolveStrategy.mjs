import stampit from 'stampit';
import { NotImplementedError } from '@swagger-api/apidom-error';
const ResolveStrategy = stampit({
  props: {
    name: null
  },
  methods: {
    canResolve() {
      return false;
    },
    async resolve() {
      throw new NotImplementedError('resolve method in ResolveStrategy stamp is not yet implemented.');
    }
  }
});
export default ResolveStrategy;